package test

import (
	"testing"
	"TransactionTest/internal/delivery/validator"
)

func TestValidateAddress(t *testing.T) {
	// Arrange
	// validator := validator.NewValidator()
	// Act
	// valid := validator.ValidateAddress("some-address")
	// Assert
	// if !valid { t.Error("expected valid address") }
}

// Аналогично для других публичных методов валидатора 